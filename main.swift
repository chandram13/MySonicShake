//
// Created by Marvish Chandra on 8/11/22.
//

import Foundation

class uniqueShake {

    func combineShake {
        var sizes = ["Mini","Small","Medium","Large"]
        var mySize = []
        var sonicBlast = ["Reese's Peanut Butter Cups","M&M's Minis Chocolate Candies","OREO Cookie Pieces","Snickers Bars","Butterfinger Pieces","Chocolate Chip Cookie Dough"]
        var flavors = ["Churro", "Strawberry Cheesecake", "Cheesecake", "OREO Cheesecake", "OREO Chocolate", "OREO & Reese's Peanut Butter", "Strawberry", "Chocolate", "Fresh Banana", "Vanilla", "Reese's Peanut Butter", "Hot Fudge", "Caramel"]
        var myShake = []
        var wantmyShake = ""
        var wantmyBlast = ""
        if wantmyBlast == "yes":
        while i = 0; myShake > i.length; i++{
            sonicBlast[i] += myShake
            break;
        while i = 0; mySize > sizes.length; i++{
                sizes[i] += mySize
                break;
            }
        else print("Suggest to the user that they might want to order a different drink or a shake.")
        }
        if wantmyShake == "yes":
        while i = 0; myShake > i.length; i ++ {
            flavors[i] += myShake
            break;
        while i = 0; mySize > sizes.length; i++{
                sizes[i] += mySize
                break;
            }
        }
        else print("Suggest to the user that might want to order a different drink or Sonic Blast.")
    }
}
    struct Additions {
        myObj1 = currentItems
        myObj2 = mixIns
        myObj3 = toppings
        myObj4 = flavorings
    }




